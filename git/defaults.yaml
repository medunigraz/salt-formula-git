{% load_yaml as rawmap %}
Debian:
  pkgs:
    - git-core
  config:
    manage:
      - global
    global:
      name: /etc/gitconfig
RedHat:
  pkgs:
    - git
  config:
    manage:
      - global
    global:
      name: /etc/gitconfig
{% endload %}
